package GenerateRule;
/**
 *enum reserved words
 **/
public enum EReservedWords {
	ABSTRACT, ALL, AND, AS, ASSERT, BUT, CHECK,
	DISJ, ELSE, EXACTLY, EXTENDS, FACT, FOR, FUN,
	IDEN, IFF, IMPLIES, IN, INT, LET, LONE, MODULE,
	NO, NONE, NOT, ONE, OPEN, OR, PRED, RUN, SET, SIG,
	SOME, SUM, UNIV;
}
